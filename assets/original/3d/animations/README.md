# Shared animation contract

The rigged GLB files embed `idle`, `run`, `attack`, `cast`, `hit`, and `death` on the same
13-bone `shared_humanoid_rig`. The deterministic source is
`../sources/generate_lowpoly_assets.py`; this directory is reserved for future external animation
libraries when real content proves they reduce duplication further.
